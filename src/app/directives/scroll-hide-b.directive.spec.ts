import { ScrollHideBDirective } from './scroll-hide-b.directive';

describe('ScrollHideBDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollHideBDirective();
    expect(directive).toBeTruthy();
  });
});
